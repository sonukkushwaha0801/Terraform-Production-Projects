metadata:
  id: CUSTOM_AWS_504
  name: "Ensure Environment tag exists"
  category: "GENERAL_SECURITY"

definition:
  cond_type: attribute

  resource_types:
    - aws_s3_bucket

  attribute: tags.Environment

  operator: exists
