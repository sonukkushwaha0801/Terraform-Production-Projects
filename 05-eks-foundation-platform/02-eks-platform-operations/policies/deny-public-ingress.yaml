metadata:
  id: CUSTOM_PUBLIC_INGRESS
  name: Deny Public Exposure
  category: NETWORKING

definition:
  cond_type: attribute
  resource_types:
    - aws_security_group
  attribute: ingress.cidr_blocks
  operator: not_contains
  value: "0.0.0.0/0"
